Upgrade the preview/demo websites so the EN / ES language toggle actually switches the entire site content between English and Spanish.

Right now the preview site has an EN/ES toggle, but the generated preview content is not fully translated. I need the entire preview site to support bilingual content so the user can toggle between English and Spanish instantly.

Goal:
Every preview website generated by the demo builder should support full bilingual switching with the EN / ES toggle.

Requirements:

1. All visible text on the preview website must support both English and Spanish versions, including:
   - hero headline
   - hero subheadline
   - CTA buttons
   - about section
   - services titles and descriptions
   - service cards
   - gallery captions
   - portfolio titles and descriptions
   - testimonials / reviews
   - FAQ questions and answers
   - section headings
   - navigation labels
   - footer text
   - contact section text
   - form labels
   - placeholder text
   - success messages

2. The demo payload must support bilingual values.

Update the payload structure so fields can store both languages:

Example:

{
  businessName: "Charlotte Painting Pro",
  trade: "painting",
  clientFirstName: "Carlos",

  hero: {
    headline: {
      en: "Professional House Painting in Charlotte",
      es: "Pintura profesional de casas en Charlotte"
    },
    subheadline: {
      en: "Interior & exterior painting you can trust.",
      es: "Pintura interior y exterior en la que puede confiar."
    }
  },

  services: [
    {
      title: {
        en: "Interior Painting",
        es: "Pintura Interior"
      },
      description: {
        en: "Transform your home with expert interior painting.",
        es: "Transforme su hogar con pintura interior profesional."
      }
    }
  ]
}

3. Update all preview components so text is rendered using the active language.

Example logic:

currentLanguage = "en" or "es"

displayText = content[currentLanguage]

Do not hardcode English text directly into components.

4. Implement a global language state system.

Use a simple global language context or hook.

Example:
LanguageContext or useLanguage()

This should allow the entire preview site to switch languages instantly.

5. The language toggle must:

- switch between EN and ES
- update the language state
- rerender the page content
- not require a page refresh
- persist the selected language (localStorage)

Example:
localStorage.setItem("siteLanguage", "es")

6. Trade templates must include bilingual content.

All trade templates should include both English and Spanish versions for:

- hero text
- service titles
- service descriptions
- about text
- CTA text
- gallery captions
- portfolio titles
- FAQs

7. Client overrides must support bilingual input.

The demo builder may initially only collect English text, but the system should allow:

priority order:

1. client bilingual override
2. client English value auto-translated to Spanish
3. trade template bilingual content
4. generic bilingual fallback

8. Automatic translation fallback

If the builder only supplies English text, automatically generate Spanish using DeepL or a translation helper.

Example:

if (content.es missing)
  content.es = translate(content.en)

Do this once when generating the preview payload.

9. Gallery and portfolio captions must also support bilingual content.

Example:

galleryItem = {
  imageUrl: "...",
  caption: {
    en: "Freshly painted living room",
    es: "Sala recién pintada"
  }
}

10. Navigation labels must switch languages as well.

Examples:

EN:
Services
Gallery
Portfolio
About
Reviews
Contact

ES:
Servicios
Galería
Portafolio
Nosotros
Reseñas
Contacto

11. Make sure the language toggle works across all tiers:

- Empieza
- Crece
- Domina

No page should remain English-only.

12. Ensure all preview pages respect the language state, including:

- homepage
- services page
- gallery page
- portfolio page
- about page
- contact page

13. Search the entire preview codebase for any remaining hardcoded English text and convert it into bilingual fields or translation keys.

Do not leave English-only text in components.

14. Do NOT redesign the UI.

Only modify content binding and translation logic.

15. Performance requirement

Do not translate text on every render. Cache translations in the demo payload.

16. Ensure Spanish supports special characters properly:

- á
- é
- í
- ó
- ú
- ñ
- ¿
- ¡

17. Maintain SEO structure.

If metadata exists (title/description), allow bilingual versions.

After completing the work, report:

1. All files updated
2. How language state is managed
3. How translations are stored in the payload
4. How trade templates now support bilingual text
5. Where automatic translation occurs
6. Whether any hardcoded English still remains
7. A test checklist

Test checklist:

- generate preview
- open preview
- toggle EN/ES
- verify hero text switches
- verify services switch
- verify gallery captions switch
- verify portfolio text switches
- verify navigation switches
- verify footer switches
- verify toggle persists on refresh
- verify it works on Empieza, Crece, and Domina

Success criteria:

The entire preview website can toggle between English and Spanish instantly, with all text translating correctly across every page and section.