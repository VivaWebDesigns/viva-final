IMPORTANT IMPLEMENTATION RULES

- This work must be NONDESTRUCTIVE.
- Do NOT remove or break any existing routes, screens, or data flows.
- All new architecture must be introduced alongside existing logic using adapters or parallel structures.
- Prefer additive refactors over destructive rewrites.
- Maintain full backward compatibility.
- Keep strict TypeScript typing across all new modules.
- NO “any” types unless absolutely unavoidable.
- Route files MUST remain thin (validation + service call only).
- ALL cross-domain data composition must live in a dedicated service/support layer.
- DO NOT scatter composition logic across routes or frontend pages.
- Reuse existing database schema unless explicitly instructed otherwise.
- Do NOT introduce breaking migrations in this phase.
- Every new module must include:
  - clear types
  - clear responsibility boundaries
  - inline documentation
- Update App Docs in Super Admin:
  - architecture
  - data flow
  - DTO contracts
  - migration plan
- Add tests wherever practical (especially for service layer logic).
- After completion:
  - app must compile
  - no regression in existing functionality

--------------------------------------------------------------------------------

OBJECTIVE

We are fixing a core architectural issue in the CRM:

The same real-world entity (company/contact/lead/opportunity) is assembled differently across:
- Lead detail
- Opportunity detail
- Client profile

We must introduce a **canonical unified profile model** WITHOUT breaking existing functionality.

--------------------------------------------------------------------------------

PHASE GOAL

Establish a NEW PROFILE DOMAIN that will become the foundation for:
- unified profile reads
- shared UI shell
- consistent data modeling across CRM

This phase DOES NOT modify UI or routes yet.

--------------------------------------------------------------------------------

STEP 1 — DOMAIN AUDIT (REQUIRED)

Analyze and document relationships across:

- crm_companies
- crm_contacts
- crm_leads
- pipeline_opportunities
- followup_tasks
- crm_lead_notes
- client_notes
- pipeline_activities
- onboarding (if exists)
- billing/files (if exists)

Output (internal dev notes + App Docs):
- entity relationship map
- ownership hierarchy (company as root, etc.)
- where duplication or fragmentation occurs

--------------------------------------------------------------------------------

STEP 2 — CREATE PROFILE DOMAIN STRUCTURE

Create:

server/features/profiles/

Required files:

- service.ts
- dto.ts
- types.ts
- mappers.ts
- relationships.ts
- index.ts

OPTIONAL (if needed):
- repository.ts (read aggregation helpers only, NOT CRUD duplication)

--------------------------------------------------------------------------------

STEP 3 — DEFINE CANONICAL DTO (CRITICAL)

Create UnifiedProfileDto with STRICT typing.

It MUST include:

identity:
- company
- primaryContact
- contacts[]

sales:
- sourceLead
- leadHistory[]
- activeOpportunity
- opportunities[]

work:
- tasks[]
- nextAction

timeline:
- events[] (placeholder for now)

service:
- onboarding[]
- billingSummary
- files[]

derived:
- owner
- status
- health
- stage
- value
- lastActivityAt

ALSO CREATE:

- UnifiedTimelineEvent
- UnifiedProfileSummary
- ProfileContextType = "company" | "lead" | "opportunity"

--------------------------------------------------------------------------------

STEP 4 — RELATIONSHIP RESOLUTION LAYER

Create relationships.ts that defines:

- how a lead resolves to:
  - company
  - contact
  - opportunity

- how an opportunity resolves to:
  - company
  - contact
  - source lead

- how company aggregates:
  - contacts
  - leads
  - opportunities

This file should act as a SINGLE SOURCE OF TRUTH for entity relationships.

--------------------------------------------------------------------------------

STEP 5 — SERVICE SKELETON (NO FULL IMPLEMENTATION YET)

In service.ts:

Create skeleton methods:

- getProfileByCompanyId(companyId)
- getProfileByLeadId(leadId)
- getProfileByOpportunityId(opportunityId)

Each should:
- outline composition steps
- call relationship layer
- return PARTIAL DTO safely

DO NOT fully implement yet—just structure it.

--------------------------------------------------------------------------------

STEP 6 — TYPE SAFETY + MAPPERS

In mappers.ts:

- Create transformation functions:
  - mapCompanyToProfileIdentity
  - mapLeadToSalesData
  - mapOpportunityToSalesData

Ensure:
- no raw DB rows leak into DTO
- all outputs match DTO shape exactly

--------------------------------------------------------------------------------

STEP 7 — APP DOCS (MANDATORY)

Create:

Super Admin > App Docs > CRM > Profiles Architecture

Include:
- why this refactor exists
- DTO definition
- relationship mapping
- future migration plan
- how this replaces fragmented logic

--------------------------------------------------------------------------------

ACCEPTANCE CRITERIA

- Profile domain exists with clean structure
- DTO is fully defined and typed
- No UI or route changes yet
- No regression in system behavior
- App builds successfully
- App Docs updated

--------------------------------------------------------------------------------

DO NOT:

- modify existing routes
- modify frontend
- duplicate CRUD logic
- introduce new database schema